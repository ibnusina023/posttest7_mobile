import 'package:flutter/material.dart';
import 'package:get/get.dart';

class baris1 extends StatelessWidget {
  const baris1({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 200,
      height: 200,
      child: new Image.asset('assets/photo1.png')
    );
  }
}
class baris2 extends StatelessWidget {
  const baris2({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 200,
      height: 200,
      child: new Image.asset('assets/photo2.png')
    );
  }
}

class baris3 extends StatelessWidget {
  const baris3({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 200,
      height: 200,
      child: new Image.asset('assets/photo3.png')
    );
  }
}

class baris4 extends StatelessWidget {
  const baris4({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 200,
      height: 200,
      child: new Image.asset('assets/photo4.png')
    );
  }
}